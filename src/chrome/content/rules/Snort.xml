<!--
Disabled by https-everywhere-checker because:
Fetch error: http://snort.org/ => https://snort.org/: (51, "SSL: no alternative certificate subject name matches target host name 'snort.org'")
	Nonfunctional subdomains:

		- blog *
		- vrt-blog *

	* blogger

-->
<ruleset name="Snort (partial)" default_off='failed ruleset test'>

	<target host="snort.org" />
	<target host="*.snort.org" />
		<!--exclusion pattern="^http://(blog|vrt-blog)\.snort\.com/" /-->


	<securecookie host=".+\.snort\.org$" name=".+" />


	<rule from="^http://(store\.|www\.)?snort\.org/"
		to="https://$1snort.org/" />

</ruleset>
